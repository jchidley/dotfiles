# rg and fd use regex searching semantics or file/directory glob
history -a # add current history to .bash_history file
history
!number # execute history command number
# McFly used for Ctrl-R history search
~/.local/share/chezmoi
#---
bw login jack@chidley.org --method 0
bw unlock # then use this to unlock the session...
# After unlock, copy the session token and run:
# export BW_SESSION="<paste-session-token-here>"
# Or use: export BW_SESSION=$(bw unlock --raw)
bw get password \"surface laptop ssh - Silver\"
#---
wt.exe -w last new-tab -p "Debian" \; split-pane -V -p "Debian"
set-title "Amazing Title"
Python: use uv or uvx # curl -LsSf https://astral.sh/uv/install.sh | sh
llm or aichat for LLM
cd /tmp
~/ps_shell_hints
wslpath "C:\Users\jackc\Downloads" # /mnt/c/Users/jackc/Downloads
