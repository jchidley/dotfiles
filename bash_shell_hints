# Shell Hints
# -----------
# Search:     rg, fd (regex/glob)
# History:    history -a, history, !<num>, Ctrl-R (McFly)
# Dotfiles:   ~/.local/share/chezmoi

# Secrets
bw_unlock              # Unlock Bitwarden + load API keys
bw get password "surface laptop ssh - Silver"

# WSL/Windows
wt.exe -w last new-tab -p "Debian" \; split-pane -V -p "Debian"
set-title "Title"
wslpath "C:\Users\jackc\Downloads"  # → /mnt/c/Users/jackc/Downloads

# Tools
uv / uvx               # Python (curl -LsSf https://astral.sh/uv/install.sh | sh)
llm / aichat           # LLM CLI tools
