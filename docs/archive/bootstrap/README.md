# Retired bootstrap

`debian-bootstrap-legacy.sh` is retained only as historical evidence of the former GitHub-wide, token-in-URL workflow. Do not execute it. Current rebuilds use `scripts/bootstrap/debian-bootstrap-safe.sh` and `workspace-repos.tsv`.
