# rg and fd use regex searching semantics or file/directory glob
(Get-PSReadlineOption).HistorySavePath
$env:USERPROFILE/.local/share/chezmoi
Documents\PowerShell\Microsoft.PowerShell_profile.ps1 # in $env:USERPROFILE/.local/share/chezmoi
gci env: #environment variables
Get-Content $env:USERPROFILE/.config/ak/vault.conf # authoritative named WSL distro for GPG-backed ak
Get-Content $env:USERPROFILE/.envrc # reviewed service allowlist imported into WSL direnv and PowerShell
ak-list # list the configured named distro's ak services
ak-get <service> # explicit on-demand GPG access; no default-distro or Credential Manager fallback
# Bitwarden is human-managed and must not be automated by agents.
wt -w last sp -V -p "Debian" `; mf left `; sp -H
wt -w last sp -V `; new-tab -p "Debian" `; split-pane -V -p "Debian"
wt -w last new-tab -p "Debian"
rm $env:USERPROFILE/AppData/Local/helix/helix.log # gets stupidly large
Python: use uv or uvx # powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
llm or aichat for LLM
cd $env:TEMP
~/bash_shell_hints
# SMB/iPhone file transfer: enable Windows File and Printer Sharing on Private networks, then disable after use
sudo powershell.exe -NoProfile -ExecutionPolicy Bypass -Command "Set-NetFirewallRule -DisplayGroup 'File and Printer Sharing' -Enabled True -Profile Private"
sudo powershell.exe -NoProfile -ExecutionPolicy Bypass -Command "Set-NetFirewallRule -DisplayGroup 'File and Printer Sharing' -Enabled False -Profile Private"
# Network Discovery can stay off if connecting directly, e.g. smb://black/AppleShare
